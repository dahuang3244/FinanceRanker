# ---- Core service ----
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12

# ---- Crawling / HTTP ----
# akshare is the primary crawler facade requested by the project owner.
akshare>=1.18
requests>=2.32
pypdf>=5.0              # TSMC issuer PDF cash-flow fallback
curl_cffi>=0.7          # TLS-impersonation fallback (helps when a source blocks plain python-requests)
tenacity>=9.0           # retry/backoff policy shared by every provider

# ---- Data / compute ----
pandas>=2.2
numpy>=1.26

# ---- Output ----
openpyxl>=3.1           # CSV + XLSX export (same template as Free_Public_Data_Tech_Ranker.xlsx)

# ---- Storage / scheduling ----
apscheduler>=3.10

# ---- Optional: enable the Yahoo Finance provider when a proxy / overseas host is available ----
# yfinance>=0.2.40

# ---- Packaging (optional: build a standalone desktop app) ----
# See finance_ranker.spec and README §7.5
pyinstaller>=6.10
# Desktop window. Windows does not use pywebview at all -- the packaged app
# drives the Chromium bundled into the build -- so pythonnet, whose
# Python.Runtime.dll cannot be loaded from a frozen bundle, is never pulled in
# there. macOS keeps pywebview (WKWebView), which has no such dependency.
pywebview>=5.3; sys_platform != "win32"
